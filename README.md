# Camera-Calibration
This repository implements a two-stage camera calibration pipeline for metric road measurements using monocular images. It performs intrinsic calibration using Zhang’s method and estimates camera pose and ground-plane homography using planar constraints. The pipeline enables mapping image pixels to real-world coordinates (in meters).
